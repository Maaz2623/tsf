import FullscreenLoader from "@/components/fullscreen-loader";
import React from "react";

const Loading = () => {
  return <FullscreenLoader title="Loading events..." />;
};

export default Loading;
